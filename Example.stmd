* Load in the data
use “”, clear
codebook 
* Create a local variable of demographics
local demogvars = race gender
* Get summary stats in a loop
foreach myvar in varlist `demogvars’ {
tabstat `myvar’, stats(n mean sd min max)
}

